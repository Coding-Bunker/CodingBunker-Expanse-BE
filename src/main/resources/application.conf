ktor {
    deployment {
        port = 8080
        port = ${?PORT}

        shutdown.url = "/ktor/application/shutdown"
    }
    application {
        modules = [ it.codingbunker.tbs.ApplicationKt.module ]
    }
    mongoDB {
        address = "mongodb://127.0.0.1:27017"
    }
}