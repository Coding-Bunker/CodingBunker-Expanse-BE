ktor {
    deployment {
        port = 8080
        port = ${?PORT}

        shutdown.url = "/ktor/application/shutdown"
    }

    mongoDB {
        address = "mongodb://127.0.0.1:27017"
        nameDB = "takao_expanse_db_test"
    }
}